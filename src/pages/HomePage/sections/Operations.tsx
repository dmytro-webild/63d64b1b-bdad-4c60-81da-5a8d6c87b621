/* eslint-disable */
// @ts-nocheck — generated by catalog-eject; runtime-correct but TS strict-mode false-positives on inlined catalog body
import Button from "@/components/ui/Button";
import TextAnimation from "@/components/ui/TextAnimation";
import GridOrCarousel from "@/components/ui/GridOrCarousel";
import ScrollReveal from "@/components/ui/ScrollReveal";

const metrics = [
  {
    value: "$99,999",
    description: "Professional Dispatchers"
  },
  {
    value: "$9,999",
    description: "Avg. Per Truck"
  },
  {
    value: "$99,999",
    description: "Professional Dispatchers"
  },
  {
    value: "16 ",
    description: "Professional Dispatchers"
  }
];

type Metric = {
  value: string;
  description: string;
};

const OperationsInline = () => (
  <section aria-label="Metrics section" className="py-20">
    <div className="flex flex-col gap-8 md:gap-10">
      <div className="flex flex-col items-center gap-2 w-content-width mx-auto">
        <div className="px-3 py-1 mb-1 text-sm card rounded w-fit">
          <p>{"Live Operations"}</p>
        </div>

        <TextAnimation
          text={"Operational Performance"}
          variant={"slide-up"}
          gradientText={true}
          tag="h2"
          className="md:max-w-8/10 text-6xl 2xl:text-7xl leading-[1.15] font-semibold text-center text-balance"
        />

        <TextAnimation
          text={"Real-time metrics showing our dispatch impact."}
          variant={"slide-up"}
          gradientText={false}
          tag="p"
          className="md:max-w-7/10 text-lg md:text-xl leading-snug text-center text-balance"
        />

        {(undefined || undefined) && (
          <div className="flex flex-wrap justify-center gap-3 mt-2 md:mt-3">
            {undefined && <Button text={undefined.text} href={undefined.href} variant="primary"/>}
            {undefined && <Button text={undefined.text} href={undefined.href} variant="secondary"animationDelay={0.1} />}
          </div>
        )}
      </div>

      <ScrollReveal variant="slide-up">
        <GridOrCarousel>
          {metrics.map((metric) => (
            <div key={metric.value} className="flex flex-col justify-between gap-6 p-6 md:p-10 min-h-60 md:min-h-70 2xl:min-h-80 h-full card rounded">
              <span className="text-7xl md:text-6xl font-semibold leading-none truncate">{metric.value}</span>
              <p className="text-base leading-snug text-balance">{metric.description}</p>
            </div>
          ))}
        </GridOrCarousel>
      </ScrollReveal>
    </div>
  </section>
);

export default function OperationsSection() {
  return (
    <div data-webild-section="operations" data-section="operations" id="operations">
      <OperationsInline />
    </div>
  );
}
